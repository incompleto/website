{
  "name": "surfclub",
  "description": "Surf Club website",
  "version": "0.0.1",
  "license": "MIT",
  "main": "src/index.html",
  "scripts": {
    "start": "parcel src/*.html --open --host 0.0.0.0",
    "build": "PARCEL_WORKERS=1 NODE_ENV=production parcel build src/*.html --public-url ./"
  },
  "dependencies": {
    "cssnano": "^4.1.10",
    "parcel-bundler": "^1.12.4"
  }
}
